{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|Write|Edit|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "python3 /home/sarturko/projects/tabular_review/src/pipeline/check_requests.py 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); n=d.get('pending',0); reqs=d.get('requests',[]); summary='\\n'.join(f'  - [{r[\\\"type\\\"]}] id={r[\\\"id\\\"]}: {r[\\\"prompt\\\"][:120]}' for r in reqs); ctx=f'[UI REQUESTS] {n} pending request(s) from the UI:\\n{summary}\\n\\nProcess these immediately by reading the request files in data/output/requests/ and following the instructions in CLAUDE.md for each type.'; print(json.dumps({'hookSpecificOutput':{'hookEventName':'PostToolUse','additionalContext':ctx}})) if n > 0 else None\" 2>/dev/null || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
